name: building_manager
description: Приложение для управления строительными объектами
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.1
  shared_preferences: ^2.2.2  # Вместо sqflite для кроссплатформенности
  intl: ^0.19.0
  uuid: ^4.4.0
  image_picker: ^0.8.7+7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true